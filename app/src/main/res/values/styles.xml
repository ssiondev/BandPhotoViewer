<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Widget.CardContent" parent="android:Widget">
        <item name="android:typeface">sans</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="Font" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">#424242</item>
        <item name="android:typeface">sans</item>
    </style>

    <style name="ExpandedTextApperance" parent="@android:style/TextAppearance.Large">
        <item name="android:textColor">#757575</item>
        <item name="android:textSize">80sp</item>
    </style>

    <style name="CollapsedTextApperance" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">#757575</item>
        <item name="android:textSize">30sp</item>
    </style>

</resources>
